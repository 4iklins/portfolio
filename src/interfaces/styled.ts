export interface ITheme {
  colors: {
    bgPrimary: string;
    bgSecondary: string;
    accent: string;
    font: string;
    border: string;
    paceholder: string;
  };
  media: {
    tablet: string;
    mobile: string;
    desktop: string;
  };
}
