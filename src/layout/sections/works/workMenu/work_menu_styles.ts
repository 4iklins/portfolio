import styled from 'styled-components';

const WorkMenu = styled.nav`
  ul {
    display: flex;
    justify-content: space-between;
    max-width: 330px;
    margin: 0 auto 40px;
  }
`;

const MenuItem = styled.li``;

export const S = {
  WorkMenu,
  MenuItem,
};
