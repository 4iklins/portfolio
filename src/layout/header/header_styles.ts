import styled from 'styled-components';

const Header = styled.header`
  background-color: rgb(31, 31, 32, 0.9);
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 100;
`;

export const S = {
  Header,
};
